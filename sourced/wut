# Custom Bash stuff that I don't understand.

# What do these do?
#xhost +local:root > /dev/null 2>&1 # Something to do with adding permissions for the X server, which might not be what's really wanted.
complete -cf sudo # Probably making command completion work with sudo.

# And these?
shopt -s cdspell
shopt -s checkwinsize
shopt -s dotglob
shopt -s expand_aliases
shopt -s extglob
shopt -s hostcomplete
shopt -s nocaseglob
